"use client";
import{useState}from"react";
export function SignInForm(){const[email,setEmail]=useState("");const[sent,setSent]=useState(false);const[busy,setBusy]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();if(busy)return;setBusy(true);await new Promise(r=>setTimeout(r,300));setSent(true);setBusy(false)};if(sent)return <div className="signin-sent"><span>✉</span><h2>Revisa tu correo</h2><p>Si <strong>{email}</strong> puede iniciar sesión, recibirá un enlace válido durante 15 minutos.</p><button onClick={()=>setSent(false)}>Usar otra dirección</button></div>;return <form className="signin-form" onSubmit={submit}><label>Correo electrónico<input required type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="tu@correo.com"/></label><button disabled={busy}>{busy?"Enviando…":"Continuar con correo"}</button><div className="or"><span>o</span></div><button type="button" className="google-button"><b>G</b> Continuar con Google</button><p>Al continuar aceptas el aviso de privacidad. Tener una cuenta no concede automáticamente membresía del club.</p></form>}
