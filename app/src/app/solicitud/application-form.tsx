"use client";
import{useMemo,useState}from"react";import{playerApplicationSchema}from"@/server/players/validation";
export function ApplicationForm(){const[data,setData]=useState({fullName:"",preferredLocale:"es-MX",selfAssessedLevel:"",dominantHand:"" as ""|"left"|"right",preferredSide:"" as ""|"left"|"right"|"either"});const[consent,setConsent]=useState(false);const[sent,setSent]=useState(false);const parsed=useMemo(()=>playerApplicationSchema.safeParse({...data,dominantHand:data.dominantHand||undefined,preferredSide:data.preferredSide||undefined}),[data]);const field=<K extends keyof typeof data>(key:K,value:(typeof data)[K])=>setData(x=>({...x,[key]:value}));if(sent)return <section className="application-success"><span>✓</span><p className="eyebrow">SOLICITUD RECIBIDA</p><h2>El club revisará tu perfil</h2><p>Crear una cuenta no concede membresía ni acceso a eventos protegidos. Te avisaremos cuando un administrador verifique tu nivel.</p><div><strong>Estado</strong><span>Pendiente de revisión</span></div></section>;return <form className="application-form" onSubmit={e=>{e.preventDefault();if(parsed.success&&consent)setSent(true)}}><label>Nombre completo<input autoComplete="name" value={data.fullName} onChange={e=>field("fullName",e.target.value)} placeholder="Como aparecerá en partidos"/></label><div className="form-row"><label>Idioma preferido<select value={data.preferredLocale} onChange={e=>field("preferredLocale",e.target.value)}><option value="es-MX">Español</option><option value="en">English</option></select></label><label>Tu nivel actual<select value={data.selfAssessedLevel} onChange={e=>field("selfAssessedLevel",e.target.value)}><option value="">Selecciona</option><option>Principiante</option><option>Intermedio</option><option>Avanzado</option></select></label></div><div className="form-row"><label>Mano dominante<select value={data.dominantHand} onChange={e=>field("dominantHand",e.target.value as typeof data.dominantHand)}><option value="">Prefiero no indicar</option><option value="right">Derecha</option><option value="left">Izquierda</option></select></label><label>Lado preferido<select value={data.preferredSide} onChange={e=>field("preferredSide",e.target.value as typeof data.preferredSide)}><option value="">Sin preferencia</option><option value="left">Izquierdo</option><option value="right">Derecho</option><option value="either">Cualquiera</option></select></label></div><label className="consent"><input type="checkbox" checked={consent} onChange={e=>setConsent(e.target.checked)}/><span>Acepto el aviso de privacidad y que el club use estos datos para revisar mi solicitud y organizar partidos.</span></label><div className="application-note"><b>¿Qué sucede después?</b><p>Un administrador revisará tu solicitud y asignará un nivel verificado. Tu rating inicial será provisional hasta que juegues partidos oficiales.</p></div><button disabled={!parsed.success||!consent}>Enviar solicitud</button></form>}
