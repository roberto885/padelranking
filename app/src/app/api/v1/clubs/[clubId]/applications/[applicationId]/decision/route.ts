import{NextRequest,NextResponse}from"next/server";import{z}from"zod";import{readCookie,requireSameOrigin}from"@/server/http/security";import{resolveSession}from"@/server/auth/postgres-session";import{reviewApplicationPostgres}from"@/server/applications/postgres-review";
const bodySchema=z.discriminatedUnion("decision",[z.object({decision:z.literal("approved"),levelCode:z.string().min(1),reason:z.string().trim().max(500).optional()}),z.object({decision:z.literal("rejected"),reason:z.string().trim().min(8).max(500)})]);
export async function POST(request:NextRequest,{params}:{params:Promise<{clubId:string;applicationId:string}>}){try{const appUrl=process.env.APP_URL;if(!appUrl)return respond("SERVER_CONFIGURATION_ERROR",503);requireSameOrigin({origin:request.headers.get("origin"),appUrl});const token=readCookie(request.headers.get("cookie"),"punto_session");if(!token)return respond("UNAUTHENTICATED",401);const session=await resolveSession(token);if(!session)return respond("UNAUTHENTICATED",401);const parsed=bodySchema.safeParse(await request.json());if(!parsed.success)return NextResponse.json({error:{code:"VALIDATION_ERROR",message:"Revisa la decisión.",fieldErrors:parsed.error.flatten().fieldErrors}},{status:422});const{clubId,applicationId}=await params;const result=await reviewApplicationPostgres({applicationId,clubId,actorUserId:session.userId,...parsed.data,now:new Date()});return NextResponse.json({data:result},{headers:{"Cache-Control":"no-store"}})}catch(cause){const code=cause instanceof Error?cause.message:"INTERNAL_ERROR";const status=code==="FORBIDDEN"||code==="INVALID_ORIGIN"||code==="ORIGIN_REQUIRED"?403:code==="APPLICATION_NOT_FOUND"?404:code==="APPLICATION_NOT_PENDING"?409:code==="UNKNOWN_LEVEL"?422:500;return respond(code,status)}}
function respond(code:string,status:number){return NextResponse.json({error:{code,message:code==="FORBIDDEN"?"No tienes permiso para revisar solicitudes.":"No fue posible completar la decisión."}},{status,headers:{"Cache-Control":"no-store"}})}
