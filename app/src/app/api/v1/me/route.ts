import{NextRequest,NextResponse}from"next/server";import{readCookie}from"@/server/http/security";import{resolveSession}from"@/server/auth/postgres-session";import{loadCurrentUser}from"@/server/auth/current-user";
export async function GET(request:NextRequest){const token=readCookie(request.headers.get("cookie"),"punto_session");if(!token)return error();const session=await resolveSession(token);if(!session)return error();const user=await loadCurrentUser(session.userId);if(!user)return error();return NextResponse.json({data:user},{headers:{"Cache-Control":"private, no-store","Vary":"Cookie"}})}function error(){return NextResponse.json({error:{code:"UNAUTHENTICATED",message:"Inicia sesión para continuar."}},{status:401,headers:{"Cache-Control":"no-store"}})}
