import{describe,expect,it}from"vitest";import{foursomeBalance,recommendPartners,type Candidate}from"./recommend";
const at=(h:number)=>new Date(Date.UTC(2026,6,11,h));const player=(id:string,rating:number,side:Candidate["side"]="either"):Candidate=>({id,rating,side,intent:"competitive",availableFrom:at(18),availableUntil:at(21),clubLocationId:"club",discoverable:true,blockedIds:[]});
describe("matchmaking recommendations",()=>{it("ranks level and side compatibility",()=>{const result=recommendPartners(player("me",1600,"left"),[player("best",1620,"right"),player("same-side",1620,"left"),player("far",2100,"right")]);expect(result[0].candidateId).toBe("best");expect(result[0].reasons).toContain("Nivel similar")});it("excludes blocked, private, wrong-location and unavailable players",()=>{const me=player("me",1600);me.blockedIds=["blocked"];const privatePlayer={...player("private",1600),discoverable:false};const other={...player("other",1600),clubLocationId:"other"};const short={...player("short",1600),availableUntil:new Date(at(18).getTime()+30*60_000)};expect(recommendPartners(me,[player("blocked",1600),privatePlayer,other,short])).toEqual([])});it("respects mutual blocking",()=>{const candidate=player("c",1600);candidate.blockedIds=["me"];expect(recommendPartners(player("me",1600),[candidate])).toEqual([])});it("builds the most balanced foursome",()=>{const result=foursomeBalance([player("a",1800,"left"),player("b",1700,"right"),player("c",1600,"left"),player("d",1500,"right")]);expect(result.ratingDifference).toBe(0);expect(new Set(result.team1)).toEqual(new Set(["a","d"]))});it("is deterministic",()=>{const me=player("me",1600),candidates=[player("b",1600),player("a",1600)];expect(recommendPartners(me,candidates)).toEqual(recommendPartners(me,candidates))});});
